# v0.10.669 実装メモ

## v0.10.668 実機結果

- HTML開始→タイトル: 1.77秒
- Firebase初期化: 617ms
- クラウドセーブ取得: 676ms
- スタート→メイン: 72ms
- 通信条件は3G相当 / downlink 1.45Mbps / RTT 800msでも1秒台を維持。
- 起動時BGM・環境音はメイン画面表示後に解放され、起動クリティカルパスから外れた。

## 今回の変更

1. `boot()`開始直後、Firebase初期化より前に本物のタイトル画面を先行表示する。
2. 認証・クラウドセーブ取得・端末/クラウド新旧判定が終わるまでは、スタートボタンを`セーブ確認中…`として無効化する。
3. セーブ確認完了後だけ`startupSaveReady = true`にし、通常の`スタート`/`はじめから`へ切り替える。
4. `data-action="start"`側にも確認完了前の進入を拒否する二重ガードを追加。
5. 起動診断を「先行タイトル表示」と「スタート有効化」に分離。見た目の立ち上がりと安全にプレイ開始できる時刻を別々に測定可能にした。

## セーブ安全性

- クラウドセーブ読み込みは従来どおり完了を待ってからスタートを有効化する。
- 端末/クラウドの新旧判定は従来どおり維持。
- 端末側が新しい場合のクラウド書き戻しは従来どおり保存キューでバックグラウンド実行。
- セッション監視/ハートビート、複数端末検知は変更なし。

## 非変更

- セーブデータ形式
- ゲームバランス
- イベント発生条件・確率・報酬
- UIレイアウト（タイトルのスタートボタン位置を含む）
- 音場定義
- v0.10.668の起動時音声ホールド、Firebase preconnect、Service Worker遅延更新
