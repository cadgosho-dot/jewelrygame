# v0.10.667 実装メモ

## 実機診断から判明したボトルネック

v0.10.666のAndroid PWA実測では、HTML開始→タイトルが34.9秒。主因は以下。

- Firebase初期化: 2.77秒
- クラウドセーブ取得: 4.37秒
- セッション取得: 6.65秒
- 端末セーブ→クラウド同期: 24.9秒

FirestoreのWrite channelが24.3秒を占め、端末セーブの書き戻し完了をタイトル表示前にawaitしていたことが最大の待ち時間だった。

## 変更

1. `claimSession()` は起動時に開始するが、書き込み応答をawaitしない。
2. `watchSession()` は従来どおり有効化し、別端末開始の監視を維持する。
3. 端末セーブが新しい場合は端末状態を即採用する。
4. クラウド書き戻しは `saveQueue` に積み、タイトル表示・ゲーム開始をブロックしない。
5. 通常の `saveGame()` も同じ `saveQueue` の後ろに並ぶため、起動時の古いスナップショットが後から新しい状態を上書きしない。
6. `loadState()` と端末/クラウドの新旧判定は引き続きタイトル表示前に完了させる。

## 期待される効果

同じ通信条件なら、v0.10.666実測の34.9秒のうち、セッション書き込み待ちと24.9秒のクラウド書き戻し待ちはタイトル表示のクリティカルパスから外れる。残る主要待ち時間はFirebase初期化とクラウドセーブ取得。

## 非変更

- セーブデータ形式
- ゲームバランス
- UIレイアウト
- イベント仕様
- クラウド新旧判定
- 端末ローカル保存
- セッション監視
