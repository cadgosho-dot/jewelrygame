# JEWELRY×JEWELRY v0.10.599 実装内容

## 水槽ミニゲーム

- ゲーム本体の水槽データを基準に、ミニゲームを開くたびに現在の配置状態を同期します。
- 同期に使用する数値は `owned`（所持数）ではなく `inTank`（水槽内の数）です。
- 観察対象は `fish` と `plants` のみです。
- `layout` は水槽内の景観には残しますが、観察一覧には表示しません。
- iframeの読み込み直後、準備完了通知後、遅延再試行時に同期します。
- 同一オリジンで水槽エンジンへ直接接続できる場合は、水槽内の魚・水草・設置用品を現在値へ揃えます。
- 直接接続できない起動環境では、従来の `aquarium-state` メッセージ同期を維持します。
- 観察画面を後から開いた場合にも、MutationObserverでレイアウト用品や未配置項目の再表示を防止します。

## 食事メニュー

- 「アイス」のタイトルへ設定されていた専用の水色指定を削除し、他の食事カードと同じ文字色に統一しました。

## 変更していないもの

- 水槽内の魚の動き、画像、背景、環境音
- 魚・水草の所持数と水槽内配置数
- 飼育上限、死亡・枯死などの既存ルール
- 流木・石の水槽内表示
