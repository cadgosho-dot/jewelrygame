# v0.10.706 累積更新内容（v0.10.692 から直接更新）

## ログイン修正

- 既存ブラウザに残った旧Service Workerが、バージョン番号の異なる認証JSを古いキャッシュから返す問題を修正。
- ページ起動直後にService Workerを更新し、新しい制御へ切り替わった場合だけ一度再読み込み。
- JS/CSSはオンライン時に現行ファイルを優先し、完全一致する版のキャッシュをオフライン時だけ使用。
- Firebase Authの永続化を同一URL内で共有できるlocalStorage優先へ統一。
- 認証ストレージ起因のエラー時は、Firebase認証用の旧状態だけを一度消去して自動復旧。
- ゲームのセーブ、設定、進行状態は消去対象外。

## これまでの累積更新

- v0.10.693～v0.10.705で確定したイベント本文、共通UI、透明枠、横画面キャラクター配置、動画、音声、取得表示、画像差し替えを収録。
- パズーパンイベントの `intro2` / `intro3` を保存・復元対象へ追加済み。
- g-Lab.動画イベントと河原加工知識イベントの確定動画を収録。

## 更新方法

このパッケージだけを、v0.10.692 のプロジェクトへ同じ階層で上書きします。途中版の適用は不要です。

