# v0.10.587 Implementation Notes

## 対象

導入動画終了後の縦画面レイアウトを、以下の5イベントで一括修正した。

1. 通りすがりのクイズ王
2. 韓国料理の水槽イベント
3. 不動産屋の琥珀イベント
4. アンティークダイヤイベント
5. 中華料理屋イベント

## 修正内容

- 縦画面のみ、人物・イベント画像の最大高さを抑制。
- 固定配置されていた人物領域と会話枠を通常の2段グリッドへ戻した。
- イベント画面またはクイズの画面コンテンツを縦スクロール可能にした。
- Androidの下部ナビゲーション対策として、下部へ `env(safe-area-inset-bottom)` を含む余白を追加した。
- クイズ王の4択問題枠から固定最大高さを外し、最後の選択肢まで親画面でスクロールできるようにした。
- 720px以下の低い縦画面では人物画像をさらに縮小した。
- 横画面向けの既存CSSには変更を加えていない。

## バージョン更新

- `js/game-data.js`
- `sw.js`
- `index.html`
- `game.html`
- `js/app.js` の静的モジュール参照
