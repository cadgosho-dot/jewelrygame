# v0.10.598 実装メモ

## 目的
人物イベントでセリフ欄の下に不要な空間が生じ、人物画像の表示領域が狭くなる問題を、イベントごとの固定値ではなく共通処理で防止する。

## 実装
- 描画後にイベント本体、セリフ欄、人物領域を取得。
- セリフ欄の実際の上端位置を `getBoundingClientRect()` で測定。
- イベント下端からセリフ欄上端までを `--event-dialogue-reserve` として設定。
- 人物領域の下端を同変数へ合わせ、セリフ欄直上まで表示。
- セリフ欄は安全領域を残して画面下端へ固定。
- 描画直後、次フレーム、80ms後、240ms後、フォント読み込み後、画面回転・リサイズ時に再計算。

## 対象
- `visit-character-event` を使う人物イベント
- ウエスタン・ユニオン
- パズーパン
- 人魚
- 韓国料理の水槽イベント
- ホワイトバニーの選択肢画面を含む

## 対象外
- 導入動画の全画面ステージ
- 人物とセリフ欄の組み合わせを使わない特殊画面
- 報酬だけを中央表示する画面
