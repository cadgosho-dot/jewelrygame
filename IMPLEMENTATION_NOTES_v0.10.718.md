# JEWELRY×JEWELRY v0.10.718 実装メモ

## 対象
工房 → ジュエリー完成画面（`completion`）の縦向き携帯表示。

## 修正理由
v0.10.717で完成品上端の切れは解消したが、実機確認後に「完成品画像をもう少し下へ」と調整指示があった。

## 実装
- 携帯確認用HTML `lower_v2` で確認した位置を本体へ反映。
- 縦画面の完成品プレビュー内上余白を `104px` → `124px` に変更し、完成品本体・ルースを一緒に20px下へ移動。
- リング／ペンダント／ピアスすべて同じ基準で下げる。
- 完成品ステージ寸法、ルースサイズ、本文開始位置、横画面配置はv0.10.717のまま維持。
- `styles.css` と `game.html` のインラインフォールバックを同じ `124px` に揃え、キャッシュ差でも位置がずれにくい構成を維持。

## 互換性
- セーブ形式変更なし。
- `SAVE_SCHEMA_VERSION = 1` を維持。
- 完成品の計算、品質、価格、経験値、注文納品処理には変更なし。
