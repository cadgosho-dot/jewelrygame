name: stage18-final-validate
on:
  push:
    branches: [jxj-stage18-v0.10.866]
permissions:
  contents: write
jobs:
  implement-and-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: jxj-stage18-v0.10.866
          fetch-depth: 0
      - name: Gate implementation run
        id: gate
        shell: bash
        run: |
          if [ -f scripts/stage18-apply.py ]; then
            echo 'apply=true' >> "$GITHUB_OUTPUT"
          else
            echo 'apply=false' >> "$GITHUB_OUTPUT"
            echo 'Implementation already applied; validation push detected.'
          fi
      - name: Apply exact stage 18 patch
        if: steps.gate.outputs.apply == 'true'
        run: |
          python3 scripts/stage18-apply.py
          python3 scripts/version-sync.py --set 0.10.866
          python3 scripts/generate-event-probability-list.py --write
      - name: Commit implementation tree
        if: steps.gate.outputs.apply == 'true'
        run: |
          git config user.name "cadgosho-dot"
          git config user.email "cadgosho@gmail.com"
          git rm scripts/stage18-apply.py
          git add -A
          echo '=== staged files ==='
          git diff --cached --name-only
          if git diff --cached --name-only | grep -Ei '\.(png|jpe?g|webp|gif)$'; then
            echo 'Image diff detected; aborting.' >&2
            exit 1
          fi
          git commit -m "v0.10.866 食事時間不足メッセージをhelperへ分離"
      - name: Dedicated checks
        if: steps.gate.outputs.apply == 'true'
        run: |
          node --check js/app.js
          node --check js/ui/meal-time-message.js
          node tools/test-meal-time-message.mjs
          python3 scripts/check-meal-time-message.py
          python3 scripts/version-sync.py --check
          python3 scripts/generate-event-probability-list.py --check
      - name: Prepare browser audit
        if: steps.gate.outputs.apply == 'true'
        run: |
          python3 -m pip install --disable-pip-version-check pillow playwright
          python3 -m playwright install --with-deps chromium
      - name: Full current audit with browser smoke
        if: steps.gate.outputs.apply == 'true'
        env:
          JXJ_REQUIRE_BROWSER_SMOKE: '1'
        run: timeout 8m python3 -u scripts/check-current.py
      - name: Verify no image changes and push
        if: steps.gate.outputs.apply == 'true'
        run: |
          echo '=== diff from exact main baseline ==='
          git diff --name-only 77471cea96bea730cd816fc51b177e80df80307b HEAD
          if git diff --name-only 77471cea96bea730cd816fc51b177e80df80307b HEAD | grep -Ei '\.(png|jpe?g|webp|gif)$'; then
            echo 'Image diff detected; aborting.' >&2
            exit 1
          fi
          test "$(cat VERSION)" = '0.10.866'
          test ! -e scripts/stage18-apply.py
          git push origin HEAD:jxj-stage18-v0.10.866