# JEWELRY×JEWELRY v0.10.611 実装内容

## 1. 起動時JavaScriptの遅延読込

起動時に直接importしていた大容量データ3本を遅延読込へ変更しました。

- `js/daily-gems.js` 約829KB: 「今日の宝石」詳細またはプロ向け宝石学詳細を開いた時に読込
- `js/loose-gem-professional.js` 約74KB: プロ向け宝石学詳細を開いた時に読込
- `js/kaitenzushi-embedded.js` 約64KB: 回転寿司を開始した時に読込
- 新規 `js/daily-gems-index.js` 約18KB: 朝表示に必要な365日のID・名称だけを保持

これにより、起動直後の直接読込対象から約0.95MBの専門データを外し、約18KBの軽量インデックスへ置き換えます。

## 2. Service Workerの初期キャッシュ軽量化

- install時は `CORE_SHELL` のみキャッシュ
- 画像・音声・動画・ミニゲーム素材の一括プリキャッシュを廃止
- JavaScript/CSSはキャッシュ優先
- 画面遷移で必要になった画像・音声は従来どおりランタイムキャッシュ
- バージョン更新時は新しいAPP_CACHEを使用

## 3. 動画再生のキャッシュ改善

- 動画用MEDIA_CACHEを追加
- 完全な動画レスポンスがキャッシュ済みの場合はRangeリクエストへ206で返せるよう対応
- 動画を最後まで再生した後、アイドル時に同じ動画を端末キャッシュへ格納する経路を追加
- 初回再生中に別の全動画ダウンロードを重ねる処理は追加していない

## 4. セーブ・1日終了処理の軽量化

- `saveLocalBackup()` で生成したJSONを一度だけparseし、そのスナップショットをクラウド保存へ再利用
- `saveGame()` 側の追加 `structuredClone(state)` を削除
- `firebase-service.js` 側の追加 `structuredClone(state)` を削除し、保存専用スナップショットをそのまま利用
- 就寝前チェックポイントはローカル保存だけとし、日次確定直後のクラウド保存と重複させない
- 日次結果画面を描画へ渡した後に保存を開始
- ショーケース日次処理では完成品IDのMapを作り、slotごとの全配列 `find()` を削減

## 5. 変更していないもの

ユーザー指定により、演出上の固定待ち時間は変更していません。

- 就寝処理の420ms
- 120ms
- 580ms
- 翌日切替の2秒暗転
- その他、既存イベント演出の表示時間

ゲーム内容、販売確率、イベント確率、セーブデータ形式、画面デザインも変更していません。
