# JEWELRY×JEWELRY v0.10.705 累積更新内容（v0.10.692から）

この更新は、v0.10.692以降の正式更新と、このチャットで承認されたイベント仕様を1本へ統合した直接更新用データです。

## v0.10.693〜v0.10.702の統合内容

- 工房Lv.8の必須工具から、購入不能なパソコン・CADソフト・3Dプリンターを除外。
- エメラルド班班長／ケバブイベントの承認済み表示を正式統合。
- 横画面キャラクター配置、透明セリフ枠、共通操作表示などの承認済みイベントUIを累積反映。
- 外来種カメイベントの承認済み透明PNG、縦横配置、動画全体表示、MOVIEスキップを正式統合。
- 「拾った手首」の専用暗BGM・環境音・効果音・縦横レイアウトを正式統合。
- パンダ音楽イベント用画像、ラーメン再会イベント用縦背景など、累積更新に必要な素材を収録。

## 今回確定した全イベント共通仕様

- 通常のセリフ枠・報酬枠・取得表示枠は、背景塗り・ぼかし・影を使わない透明表示へ統一。枠線と文字の視認性は維持。
- 今後追加するイベントも `event-dialogue-card`、`event-reward-card`、`event-status-card` の共通クラスを使用して同じ仕様を継承。
- `イベント終了` は全イベント共通の小さい左上ボタンへ統一。
- `タップして進む` は全イベント共通の右下表示・文字サイズへ統一。
- 横画面の通常キャラクターは横中央・上詰め・大きめ表示。下部セリフバーへ少し重なる配置とし、報酬／アイテム取得時にも縮小しない。
- イベント動画は映像全体を表示し、終了導線は右上の `MOVIEスキップ` に統一。
- 承認済み例外として「店舗盗難」イベントは従来仕様を維持。

## 今回確定したイベント別反映

- 映画館イベント
  - タイトルを `そうだ　映画、行こう` へ変更し、枠内を透明化。
  - 長い上映終了ボタンを削除し、右上の `MOVIEスキップ` へ統一。
  - 映画館背景では環境音を停止。上映中はゲーム側BGM・環境音を停止。
- 見習い職人との映画イベント
  - 話者名を `見習い職人` のみに統一。
  - 長い上映終了ボタンを削除し、右上の `MOVIEスキップ` へ統一。
  - 上映中はゲーム側BGM・環境音を停止。上映後の映画館背景は環境音なし。
- ウエスタン・ユニオン
  - セリフ枠と `アンティークダイヤを手に入れました` の取得表示を透明化。
  - 横画面のキャラクターを報酬時も会話時と同じ大きさに固定。
- パズーパン
  - 指定された3段階のセリフへ変更し、2段階目へプレイヤー名を差し込み。
  - 横画面のアイテム取得時もキャラクターを縮小・減光しない。
- 幽霊イベント
  - 指定画像を再トリミングし、左端の不要な縦線を除去した透明PNGへ置換。
- 誕生日の就寝イベント
  - 通常の「寝る」背景を使用し、共通の透明な下部セリフバーへ統一。
- g-Lab.訪問動画
  - g-Lab.のBGM・環境音を動画音声へ合成した承認済み動画へ置換。
  - 再生開始時に動画をミュートへ戻す旧処理を削除し、別トラックとの二重再生を防止。
- カワハラ加工知識イベント
  - 正式動画音声とg-Lab.のBGM・環境音を合成した承認済み動画へ置換。
  - `工房の加工知識に「サイズ直し」が追加されました` を報酬段階だけ画面中央へ表示。

## バージョン

- UI・データ・Service Workerキャッシュ識別子を `0.10.705` へ更新。

