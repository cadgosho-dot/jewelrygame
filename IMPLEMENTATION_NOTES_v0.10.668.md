# v0.10.668 実装メモ

## v0.10.667 実機結果

- HTML開始→タイトル: 2.02秒（v0.10.666の34.9秒から大幅短縮）
- Firebase初期化: 799ms
- クラウドセーブ取得: 460ms
- スタート→メイン: 94ms
- Firestore Write / Listen channelの長時間通信はタイトル表示後のバックグラウンド処理となり、起動のクリティカルパスから外れている。

## 今回の変更

1. `game.html`にFirebase主要3オリジンの`preconnect`を追加。
2. `audio.js`に起動時音声ホールドを追加。`switchAudio()`は現在シーンを記録するが、ホールド中はBGM・環境音のAudio生成/取得/再生を行わない。
3. スタート後、Googleログイン直後のゲーム進入、外部画面からの直接復帰時にホールドを解除して現在の音場を開始。
4. Service Workerの更新確認は、window load後最低10秒待ってから`requestIdleCallback`へ渡す。

## 非変更

- クラウドセーブ読み込みと端末/クラウド新旧判定
- セッション監視/ハートビート
- バックグラウンドクラウド保存キュー
- セーブデータ形式
- UIレイアウト
- イベント仕様・確率・報酬
- 音場定義そのもの
