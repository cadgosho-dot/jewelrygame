# JEWELRY×JEWELRY v0.10.619 実装内容

## 水槽表示条件の修正

スマートフォン内の水槽について、魚・水草は「所持しているだけ」では一覧・詳細へ出さず、実際に水槽へ入っている種類だけ表示するよう変更しました。

- 魚: `inTank > 0` の種類のみ一覧・詳細表示
- 水草: `inTank > 0` の種類のみ一覧・詳細表示
- 任意用品: `installed > 0` のものだけ表示
- 未配置種は名前・画像カード・select・詳細導線をDOMガードで除去
- 水槽の観察データも現在水槽内にいる魚・水草だけ許可

## UI維持

`styles.css` は変更していません。v0.10.618の上部バー、文字位置、下部メニュー、縦横画面、イベント表示レイアウトを維持します。
