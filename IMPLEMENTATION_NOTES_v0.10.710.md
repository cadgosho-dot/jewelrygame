# JEWELRY×JEWELRY v0.10.710 実装ノート

## 修正目的
一部の携帯端末で、ブルース酒場イベントのセリフ枠が実際の画面下端ではなくlayout viewport側の下端を基準に配置され、セリフ枠の下に大きな空白が残る問題を修正しました。

## 根本原因
ブルース酒場イベントは `position: fixed; inset: 0; height: 100%` を使用しており、既存のスマートフォン共通処理が `visualViewport.height` から計測している `--jwj-layout-height` をイベント画面の高さへ直接使用していませんでした。
ブラウザUIなどの影響でlayout viewportと実際の可視領域の高さが一致しない端末では、この差がセリフ枠下の空白として現れていました。

## 修正内容
- ブルース酒場イベント画面の `height / min-height / max-height` を `--jwj-layout-height` に統一。
- イベント内部レイヤーを親の実測高100%へ固定。
- 携帯ではセリフ枠を左右 `--jwj-edge-gap`、下端 `safe-area + --jwj-edge-gap` へabsolute固定。
- 確認版で承認された `safe-area + 6px` 基準を本体へ反映。
- 第1弾・第2弾・第3弾は同じ `.blues-juke-*` 共通レイアウトを使用するため、3段階すべてに適用。

## 変更しないもの
- イベントの発生条件・間隔
- セリフ、人物画像、背景画像
- BGM、効果音、音量設計
- ブラックダイヤ報酬と宝石学解説
- セーブスキーマ、既存セーブ移行
- 他イベントの配置

## バージョン
`v0.10.709` → `v0.10.710`
