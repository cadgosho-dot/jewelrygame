# v0.10.589 実装メモ

## 目的

導入動画を使用する5イベントで、動画を任意に飛ばして元のイベント本編へ進めるようにした。

対象:

1. 通りすがりのクイズ王
2. 韓国料理の水槽イベント
3. 不動産屋の琥珀イベント
4. アンティークダイヤイベント
5. 中華料理屋イベント

## 変更内容

- 各導入動画画面の左上へ、小さな `MOVIEスキップ` ボタンを常時表示。
- 端末の安全領域を考慮し、`env(safe-area-inset-top)` と `env(safe-area-inset-left)` を使用。
- タップ時は再生中の動画を停止し、自然終了時と同じ完了関数を呼び出す。
- 水槽イベントでは動画スキップ後に停止していたゲーム音を復帰。
- そのほかの4イベントは、従来どおりBGM・環境音を維持したまま本編へ移行。
- 中華料理イベントの動画を `ended`・`playing`・`error` の共通監視へ追加。
- 動画終了後の元のイベントレイアウトは変更していない。
- イベント条件、確率、会話、報酬、セーブデータ構造は変更していない。
