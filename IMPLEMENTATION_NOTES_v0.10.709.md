# JEWELRY×JEWELRY v0.10.709 実装ノート

## 修正目的
御徒町「手首を拾った」イベントで、下部に固定されるはずのセリフ枠が画面上部へ移動し、端末上部に隠れて読めなくなる問題を修正しました。

## 根本原因
`v0.10.705` で追加された全イベント共通CSSが、`.event-dialogue-card` 等へ `position: relative !important` を強制していました。
そのため、各イベントが個別に指定していた `position: absolute !important` が上書きされていました。

## 修正内容
- 全イベント共通ルールから `position: relative !important` を撤去。
- 共通ルールは透明化・文字影・z-index等だけを担当し、位置は各イベント固有CSSへ委譲。
- 既存の特殊配置を最終安全ロックで保護。
  - 御徒町「手首を拾った」イベント
  - 見習い職人・映画館イベント（スマホ縦画面）
  - エメラルド班班長・ルース一式表示（縦画面）
  - 観光客・木刀報酬
  - 川原・加工知識報酬
- 通常イベントの `.event-dialogue-card` は従来どおり `position: relative` を維持。
- ブルース酒場イベントを含む専用レイアウトには変更なし。

## バージョン
`v0.10.708` → `v0.10.709`
