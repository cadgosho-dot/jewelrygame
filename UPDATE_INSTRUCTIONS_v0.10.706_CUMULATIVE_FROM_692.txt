JEWELRY×JEWELRY v0.10.706 累積更新（現在 v0.10.692 用）

このフォルダの内容を、公開中プロジェクトのルートへ同じ階層のまま上書きしてください。
v0.10.693～v0.10.705 の途中データを先に入れる必要はありません。

重要:
1. auth-cache-recovery-v706.js を必ずルートへ追加してください。
2. auth.html、index.html、game.html、sw.js と js フォルダ内の対象ファイルを必ず一緒に上書きしてください。
3. assets フォルダ内の追加素材・動画・音源も、階層を変えずに配置してください。
4. 更新後、ログインエラーが出ていたブラウザは初回だけ自動再読み込みされることがあります。
5. 再読み込み後、同じURLからもう一度ログインしてください。

認証修復はFirebaseの古い認証状態だけを対象にしています。
ゲームのセーブデータは削除しません。

適用後の表示バージョン: 0.10.706

