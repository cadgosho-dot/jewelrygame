-        heartbeatTimer = setInterval(() => heartbeat(user.uid, sessionId), 300000);
+        if (sessionClaimed) {
+          stopSessionWatch = watchSession(user.uid, sessionId, handleSessionTakenOver);
+          startSessionHeartbeat(user);
+        }
         startupSaveReady = true;
         if (phoneGameReturnRequested()) {
           state = loadGame();
@@ -25641,7 +25680,7 @@ document.addEventListener('visibilitychange', () => {
     stopWhiteBunnyEventBgm({ reset: false });
     suspendAudio();
     stopGiftStatusSyncTimer();
-    if (heartbeatTimer) { clearInterval(heartbeatTimer); heartbeatTimer = null; }
+    stopSessionHeartbeat();
     return;
   }
   processAutopilotIfDue().catch((error) => console.error(error));
@@ -25664,9 +25703,10 @@ document.addEventListener('visibilitychange', () => {
     const eventState = childhoodFriendEventState();
     if (eventState.active && eventState.stage === 'eating') scheduleChildhoodFriendMealWatchdog(1200);
   }
-  if (currentUser && !needsEmailVerification(currentUser)) {
-    heartbeat(currentUser.uid, sessionId);
-    if (!heartbeatTimer) heartbeatTimer = setInterval(() => heartbeat(currentUser.uid, sessionId), 300000);
+  if (currentUser && !needsEmailVerification(currentUser) && sessionClaimed && !sessionTakenOver) {
+    // v0.10.826: 復帰直後に所有権を確認し、旧画面が新セッションを奪い返さない。
+    void sendSessionHeartbeat(currentUser);
+    startSessionHeartbeat(currentUser);
   }
 });
 
diff --git a/js/audio.js b/js/audio.js
index 9082ae1..ba4124c 100644
--- a/js/audio.js
+++ b/js/audio.js
@@ -1,4 +1,4 @@
-import { AUDIO_SCENE_DEFINITIONS, AUDIO_SCENE_KEYS, audioSceneDefinition, audioSceneUsesWeather } from './audio-scene-map.js?v=0.10.824';
+import { AUDIO_SCENE_DEFINITIONS, AUDIO_SCENE_KEYS, audioSceneDefinition, audioSceneUsesWeather } from './audio-scene-map.js?v=0.10.826';
 
 const AUDIO_DIR = './assets/audio';
 
diff --git a/js/firebase-service.js b/js/firebase-service.js
index f257eff..0cfb436 100644
--- a/js/firebase-service.js
+++ b/js/firebase-service.js
@@ -39,7 +39,7 @@ import {
 import { firebaseConfig } from './firebase-config.js';
 import { securityConfig } from './security-config.js';
 import { SAVE_KEY, chooseNewestSavedState } from './game-data-core.js';
-import { readIndexedDbSave, writeIndexedDbSave } from './local-save-storage.js?v=0.10.824';
+import { readIndexedDbSave, writeIndexedDbSave } from './local-save-storage.js?v=0.10.826';
 
 const previewMode = ['localhost', '127.0.0.1'].includes(location.hostname)
   && new URLSearchParams(location.search).get('preview') === '1';
@@ -724,28 +724,35 @@ export async function claimSession(uid, sessionId) {
 export function watchSession(uid, sessionId, onTakenOver) {
   if (previewMode || !db) return () => {};
   if (unsubscribeSession) unsubscribeSession();
-  let initialized = false;
   unsubscribeSession = onSnapshot(sessionDocRef(uid), (snapshot) => {
     if (!snapshot.exists()) return;
     const active = snapshot.data();
-    if (!initialized) {
-      initialized = true;
-      return;
-    }
+    // v0.10.826: 初回スナップショットも検査する。claimSession() 完了後に
+    // 監視を開始するため、初回から別IDなら監視開始前に他画面へ奪取された状態。
     if (active?.id && active.id !== sessionId) onTakenOver(active);
   });
   return unsubscribeSession;
 }
 
 export async function heartbeat(uid, sessionId) {
-  if (previewMode || !db) return;
+  if (previewMode || !db) return true;
   try {
-    await setDoc(sessionDocRef(uid), {
-      id: sessionId,
-      updatedAt: new Date().toISOString(),
-    }, { merge: true });
+    // v0.10.826: heartbeat はセッション所有者だけが更新できるようCAS化する。
+    // 旧画面がバックグラウンドから復帰しても、新しい画面のIDを上書きしない。
+    return await runTransaction(db, async (transaction) => {
+      const ref = sessionDocRef(uid);
+      const snapshot = await transaction.get(ref);
+      const active = snapshot.exists() ? snapshot.data() : null;
+      if (active?.id && active.id !== sessionId) return false;
+      transaction.set(ref, {
+        id: sessionId,
+        updatedAt: new Date().toISOString(),
+      }, { merge: true });
+      return true;
+    });
   } catch (error) {
     console.warn('Heartbeat failed:', error);
+    return null;
   }
 }
 
diff --git a/js/game-data-core.js b/js/game-data-core.js
index 39bfecc..3308d40 100644
--- a/js/game-data-core.js
+++ b/js/game-data-core.js
@@ -1,4 +1,4 @@
-export const VERSION = '0.10.824';
+export const VERSION = '0.10.826';
 export const SAVE_SCHEMA_VERSION = 1;
 export const DEFAULT_BIRTHDAY = '04-01';
 export const SAVE_KEY = 'jewelrygame-clean-v0.4.0';
diff --git a/js/game-data.js b/js/game-data.js
index c45f08f..4795f57 100644
--- a/js/game-data.js
+++ b/js/game-data.js
@@ -3,7 +3,7 @@
 // 大容量の既存ゲームデータ本体は game-data-core.js に保持し、内容を変更せず再利用する。
 import * as core from './game-data-core.js';
 
-export const VERSION = '0.10.824';
+export const VERSION = '0.10.826';
 export * from './game-data-core.js';
 
 // v0.10.753 balance hotfix:
diff --git a/js/memories-screen.js b/js/memories-screen.js
index 443a3e4..a3b0ec4 100644
--- a/js/memories-screen.js
+++ b/js/memories-screen.js
@@ -1,6 +1,6 @@
-import { MEMORIES_BG_LANDSCAPE, MEMORIES_BG_PORTRAIT } from './memories-backgrounds.js?v=0.10.824';
+import { MEMORIES_BG_LANDSCAPE, MEMORIES_BG_PORTRAIT } from './memories-backgrounds.js?v=0.10.826';
 
-const VERSION = '0.10.824';
+const VERSION = '0.10.826';
 const STYLE_ID = 'jxj-memories-style-v752';
 const OVERLAY_ID = 'jxj-memories-overlay';
 const LIGHTBOX_ID = 'jxj-memories-lightbox';
diff --git a/quiz-event-v2.css b/quiz-event-v2.css
index 18625ed..4a057d0 100644
--- a/quiz-event-v2.css
+++ b/quiz-event-v2.css
@@ -832,6 +832,24 @@ body[data-screen="looseShopOriginalQuizEvent"] .jxj-quiz-loose-v2 .jxj-quiz-rewa
   color:#95d887!important;
 }
 
+/* v0.10.825: 通りすがりのクイズ王だけ報酬原石画像の最大サイズを制限。
+   他のクイズイベント・問題画面・進行には影響させない。 */
+body[data-screen="okachimachiQuiz"] .jxj-quiz-king-v2 .jxj-quiz-reward-gem-v2{
+  display:block!important;
+  width:auto!important;
+  height:auto!important;
