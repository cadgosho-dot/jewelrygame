# JEWELRY×JEWELRY v0.10.716 実装ノート

基準: v0.10.710
更新日: 2026-08-18

## 1. 水槽・観察
- 固定表示: 水槽 / フィルター / ライト / ヒーター / 床砂。
- 魚・水草は `inTank > 0` の種類のみ観察一覧へ表示。
- 流木・石は `installed > 0` の種類のみ表示。
- 旧 `driftwood` / `layout_stone` は、新しい商品IDへ互換移行。
- 水槽設定: 魚負荷上限40、水草合計15株。

## 2. 熱帯魚屋
- 魚11種、水草10種、流木6種、Stone type A/B/C 各大中小の計9種を販売。
- 数量は▼/▲、短押し1ずつ、約360ms長押し後に約95ms間隔で連続変更。
- 魚は魚種上限と負荷上限、水草は15株、流木は合計3個、石は合計5個を購入上限に使用。
- 購入した魚・水草・ディスプレイは水槽へ即時反映。
- 専用の縦横背景、60秒ループBGM、環境音を使用。
- 営業時間・定休日は設定していない。
- 通常画面と同じ2段ヘッダーを使用。

## 3. おやつ大好き
- 御徒町入場時、当日最初の対象判定のみ1/30。水槽を所持し、ゲーム内時刻18:00以下が条件。日をまたげば再発生可能。
- アイス: 通常のアイス食事処理だけで時間を進め、食後1.5秒暗転後に御徒町へ戻る。
- 熱帯魚屋: 導入動画を再生し、標準位置に `MOVIEスキップ`。ショップの戻る／メイン画面操作時に「もう満足した？」を表示。「はい」で3時間経過し1.5秒暗転。

## 4. スピード・スター
- 御徒町入場時、当日最初の対象判定のみ1/100。何度でも再発生可能。
- 「ドンッ！！」は1行、タップ案内は文字の下。
- 登場後はパトカーサイレン。走り去る際に「ぴゅーー」効果音、消えた後はサイレン音量を下げ、イベント終了で停止。
- 「走り去っていく」の文字は表示しない。
- 財布イベントで所持金の `floor(所持金 / 3)` を失い、減額表示は赤文字。

## 5. パズーパン
- 採掘イベントの人物画像をユーザー指定の新キャラクターへ差し替え。黒い服の内部を消さないよう外周連結の黒背景だけを透明化。

## 6. ストーリーテラー
- 18:00以降の御徒町入場時、当日最初の対象判定のみ1/30。日をまたげば再発生可能。
- 御徒町4択クイズ50問から毎回1問をランダム選出。
- 問題画面は既存「通りすがりのクイズ王」と同系統の配置とBGMを使用。
- 正解報酬としてインベントリに「ブラり御徒町 ステッカー」を1個追加。
- 「いいとも！」は1行表示で人物を残す。
- セリフ枠は枠線あり・内部透明。報酬画面は暗い全面オーバーレイを使わず透明。ステッカーは上下に軽く動かし、縦横とも全体が切れない上限サイズに制限。

## 7. 互換性
- セーブキーと SAVE_SCHEMA_VERSION は変更していない。
- 新規イベント状態は既存 `events` オブジェクトへ追加し、未存在セーブでは既定値を補完。
- 水槽は旧IDを移行し、既存所持データを削除しない。
