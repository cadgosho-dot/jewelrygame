# JEWELRY×JEWELRY v0.10.717 実装メモ

## 対象
工房 → ジュエリー完成画面（`completion`）の携帯表示。

## 修正理由
実機の縦画面で、完成品画像の上側が2段上部バーに隠れ、リング上端などが切れて見えるケースが確認された。

## 実装
- 携帯確認用HTMLで確認した完成画面の安全余白を本体へ反映。
- 縦画面の本文開始位置を2段ヘッダー実寸 + 18px の安全余白として予約。
- 完成品プレビューは縦画面 `334px`、上側 `104px` の安全余白。
- 縦画面の完成品ステージはリング `168×168`、ペンダント `132×182`、ピアス `178×161`。
- 横画面はプレビュー `280px`、上側 `48px`、完成品ステージを確認版に統一。
- プレビューは完成画面に限り `overflow: visible` とし、上端クリップを防止。
- 外部CSSのキャッシュ差を考慮し、`game.html` に同じ安全配置のフォールバックを追加。

## 互換性
- セーブ形式変更なし。
- `SAVE_SCHEMA_VERSION = 1` を維持。
- 完成品の計算、品質、価格、経験値、注文納品処理には変更なし。
