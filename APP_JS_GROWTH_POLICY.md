# app.js 再肥大化防止ルール

## 目的

`js/app.js` は長期開発で大きくなった既存本体です。スパゲティコード整理の最終基準として、現在のサイズ **1,411,760 bytes** を上限に固定します。

このルールの目的は、既存本体を無理に大分解することではなく、**今後の新機能を app.js に積み上げて再び複雑化することを防ぐこと**です。

## 基本ルール

- 新しいイベント、施設、店舗、ミニゲーム、独立した特殊処理は、原則として `js/app.js` に直接大きく追加しない。
- 新機能は役割の分かる専用 JavaScript ファイルへ分離し、`app.js` は呼び出し・接続を中心にする。
- 既存不具合の修正、既存処理の接続変更、互換維持のための小変更は `app.js` で行ってよい。
- ただし変更後の `js/app.js` は **1,411,760 bytes を超えてはならない**。
- 既存処理を別モジュールへ安全に移して `app.js` が小さくなった場合は、その小さくなった状態を歓迎する。
- 上限値を新機能追加の都合だけで引き上げない。

## 自動検査

`scripts/check-repository-hygiene.py` が `js/app.js` のサイズを確認します。

この検査は `scripts/check-current.py` から実行されるため、GitHub Pages の公開前総合検査にも含まれます。

上限を超えた場合は公開前検査を失敗させ、新しく追加した処理を専用モジュールへ分離するよう促します。

## 今後の開発方針

スパゲティコード整理は、このガード導入をもって常設の大規模整理フェーズを終了します。

以後はゲーム制作を優先し、変更対象に触れた時だけ必要な範囲を整理・回帰保護します。既存の巨大コードを整理目的だけで一括分解する作業は行いません。
