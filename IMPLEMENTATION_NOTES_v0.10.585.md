# v0.10.585 実装メモ

- 中華料理イベントに導入動画 `assets/videos/events/mystery-chinese-meal-intro.mp4` を追加。
- イベント開始時は `video` ステージから始め、動画終了後に既存の会話進行へ移行。
- 縦画面・横画面とも中央で最大表示し、`object-fit: contain` により映像全体を切らずに表示。
- 動画再生中もBGM・環境音は継続。
- 自動再生不可端末では再生ボタンを表示し、読み込み失敗時は再試行可能。
